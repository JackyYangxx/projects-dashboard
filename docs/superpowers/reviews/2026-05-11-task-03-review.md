# Task 3 Review: Create PrevNextNav component

**Status:** APPROVE

**Reviewed Files:**
- `src/components/PrevNextNav.tsx`

**Acceptance Criteria Verification:**

| # | Criterion | Status |
|---|-----------|--------|
| 1 | Props interface: `prevId?: string`, `nextId?: string`, `currentIndex: number`, `total: number`, `onPrev: () => void`, `onNext: () => void` | PASS |
| 2 | UI shows `[← Prev] [3 / 12] [Next →]` layout | PASS - chevron_left icon, "Prev" text, counter, "Next" text, chevron_right icon |
| 3 | Prev button disabled when `prevId` is undefined (opacity-40, cursor-not-allowed) | PASS - `disabled:opacity-40 disabled:cursor-not-allowed` |
| 4 | Next button disabled when `nextId` is undefined | PASS - same disabled classes |
| 5 | Keyboard: `Alt+←` calls `onPrev`, `Alt+→` calls `onNext` | PASS - `e.altKey && e.key === 'ArrowLeft/ArrowRight'` |
| 6 | Height 48px, fixed bottom, full width, border-top, flexbox space-between | PASS - `h-12 fixed bottom-0 left-0 right-0 border-t flex items-center justify-between` |

**Additional Checks:**
- Counter correctly shows `currentIndex + 1 / total` (adjusts for 0-based index)
- TypeScript compiles without errors
- No placeholder/TODO comments
- CSS classes match design system tokens (`bg-surface-elevated`, `border-outline`, etc.)

**Verdict:** APPROVE